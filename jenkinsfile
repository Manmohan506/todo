@Library('todoshop') _

todoshop (
        COMPONENT             : 'todo',
        PROJECT_NAME          : "todoshop",
        SLAVE_LABEL           : "node",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NODEJS"
)